# Skills

Reusable agent skills for the `ticket-order-web` app live here.

Use this folder for web-specific workflows such as:

- adding a new component
- reviewing UI code with `ui-code-analysis`
- adding routing
- wiring API calls
- updating Vite configuration
- preparing the web Docker image

Every new skill should follow the app guidance in `../AGENTS.md`.
