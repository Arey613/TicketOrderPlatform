{
  "name": "ticket-order-web",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "npm run format:check && vite build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "generate:api": "openapi-generator-cli generate -i ../../../contracts/openapi/ticket-order-api/openapi.yml -g typescript-fetch -o src/generated/api --additional-properties=typescriptThreePlus=true",
    "validate:api": "openapi-generator-cli validate -i ../../../contracts/openapi/ticket-order-api/openapi.yml",
    "preview": "vite preview",
    "test": "npm run build"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^6.0.4",
    "react": "^19.2.7",
    "react-dom": "^19.2.7",
    "vite": "^8.1.5"
  },
  "devDependencies": {
    "@openapitools/openapi-generator-cli": "^2.25.2",
    "prettier": "^3.9.6"
  }
}
