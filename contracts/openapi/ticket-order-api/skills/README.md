# Skills

Reusable agent skills for the `ticket-order-api-contract` module live here.

Use this folder for contract-specific workflows such as:

- adding a new endpoint
- adding reusable schemas
- updating security schemes
- coordinating generated code updates

Every new skill should follow the contract guidance in `../AGENTS.md`.
